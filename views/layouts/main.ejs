<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PoolUp - <%= title %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        /* --- 1. CORE THEME & LAYOUT --- */
:root {
    --bg-dark-purple: #0a0122;
    --glow-purple: #9e00ff;
    --glow-blue: #00aaff;
    --text-primary: #ffffff;
    --text-secondary: #c0c0c0;
    --card-bg: rgba(22, 12, 46, 0.5);
    --border-color: rgba(255, 255, 255, 0.1);
    --danger-color: #ff4d4d;
}

* { margin: 0; padding: 0; box-sizing: border-box; }

html {
    scroll-behavior: smooth;
}

body {
    font-family: 'Inter', sans-serif;
    background-color: var(--bg-dark-purple);
    color: var(--text-primary);
    position: relative;
    line-height: 1.6;
}

body::before {
    content: ''; position: fixed; top: 0; left: 0; width: 100%; height: 100%;
    z-index: -1;
    background: 
        radial-gradient(ellipse at 70% 30%, rgba(158, 0, 255, 0.45) 0%, transparent 42%),
        radial-gradient(ellipse at 30% 70%, rgba(0, 170, 255, 0.40) 0%, transparent 42%);
    background-size: 170% 170%;
    opacity: 0.06; /* softer glow */
    animation: moveAurora 28s cubic-bezier(0.4, 0.0, 0.2, 1) infinite;
}

@keyframes moveAurora {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
}

main { min-height: calc(100vh - 142px); width: 100%; position: relative; z-index: 2; }

/* --- 2. PARTIALS (HEADER & FOOTER) --- */
.main-header {
    position: sticky; top: 0; padding: 1rem 5%;
    background: rgba(10, 1, 34, 0.7);
    backdrop-filter: blur(15px); -webkit-backdrop-filter: blur(15px);
    border-bottom: 1px solid var(--border-color);
    z-index: 1000;
}
.nav-container { display: flex; justify-content: space-between; align-items: center; max-width: 1400px; margin: 0 auto; }
.nav-logo { font-size: 1.8rem; font-weight: 700; color: var(--text-primary); text-decoration: none; text-shadow: 0 0 8px rgba(0, 170, 255, 0.7); transition: text-shadow 0.3s ease; }
.nav-logo:hover { text-shadow: 0 0 12px rgba(0, 170, 255, 1); }
.nav-links { list-style: none; display: flex; gap: 2rem; align-items: center; }
.nav-links > li { display: flex; align-items: center; }
.nav-links > li > a:not(.notif-trigger) { color: var(--text-secondary); text-decoration: none; font-weight: 500; padding: 8px 0; position: relative; transition: color 0.3s ease; }
.nav-links > li > a:not(.notif-trigger):hover { color: var(--text-primary); }
.nav-links > li > a:not(.notif-trigger)::after { content: ''; position: absolute; bottom: 0; left: 0; width: 100%; height: 2px; background: linear-gradient(90deg, var(--glow-blue), var(--glow-purple)); transform: scaleX(0); transition: transform 0.4s ease; transform-origin: center; }
.nav-links > li > a:not(.notif-trigger):hover::after { transform: scaleX(1); }
.main-footer { text-align: center; padding: 2rem 1rem; margin-top: auto; background: rgba(10, 1, 34, 0.7); border-top: 1px solid var(--border-color); color: var(--text-secondary); }
.notif-menu { position: relative; }
.notif-trigger {
    color: var(--text-secondary);
    text-decoration: none;
    cursor: pointer;
    position: relative;
    padding: 8px 0;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    line-height: 1;
    font-weight: 500;
    transition: color 0.3s ease;
}
.notif-trigger .notif-icon {
    display: inline-flex;
    align-items: center;
    line-height: 1;
}
.notif-trigger:hover { color: var(--text-primary); }
.notif-trigger::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, var(--glow-blue), var(--glow-purple));
    transform: scaleX(0);
    transition: transform 0.4s ease;
    transform-origin: center;
}
.notif-trigger:hover::after { transform: scaleX(1); }
.notif-count { position: absolute; top: -4px; right: -12px; min-width: 16px; height: 16px; border-radius: 999px; background: #ef4444; color: #fff; font-size: 10px; line-height: 16px; text-align: center; padding: 0 4px; }
.notif-dropdown { display: none; position: absolute; right: 0; top: 130%; width: 320px; max-height: 360px; overflow-y: auto; background: rgba(8, 8, 24, 0.96); border: 1px solid var(--border-color); border-radius: 10px; padding: 10px; z-index: 1200; }
.notif-menu:hover .notif-dropdown { display: block; }
.notif-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px; }
.notif-list { list-style: none; padding: 0; margin: 0; }
.notif-item { padding: 10px; border-radius: 8px; margin-bottom: 6px; text-align: left; }
.notif-item.unread { background: rgba(31, 122, 255, 0.15); }
.notif-item.read { background: rgba(255, 255, 255, 0.04); }
.notif-message { color: var(--text-primary); text-decoration: none; display: block; margin-bottom: 8px; line-height: 1.35; padding: 0; }
.notif-meta { display: flex; justify-content: space-between; align-items: center; gap: 8px; color: var(--text-secondary); font-size: 12px; }
.notif-read-label { color: #22c55e; font-weight: 600; }
.notif-mark, .notif-readall { border: none; background: transparent; color: var(--glow-blue); cursor: pointer; font-size: 12px; padding: 0; }
.notif-empty { color: var(--text-secondary); font-size: 0.95rem; }

/* --- 3. GENERIC & REUSABLE COMPONENTS --- */
.page-container { max-width: 1400px; margin: 0 auto; padding: 3rem 2rem; animation: fadeIn 1s ease; }
@keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
.page-header { text-align: center; margin-bottom: 2rem; }
.page-header h2 { font-size: 2.8rem; text-shadow: 0 0 10px rgba(0, 170, 255, 0.5); }
.page-header h3 { font-size: 1.5rem; color: var(--text-secondary); font-weight: 400; }

.btn { padding: 12px 24px; font-size: 1rem; font-weight: 600; text-decoration: none; border: 1px solid transparent; border-radius: 8px; cursor: pointer; transition: all 0.3s ease; display: inline-block; text-align: center; }
.btn-primary { color: white; background: linear-gradient(90deg, var(--glow-blue), var(--glow-purple)); border-color: transparent; }
.btn-primary:hover { transform: translateY(-3px); box-shadow: 0 4px 20px rgba(158, 0, 255, 0.5); }
.btn-danger { color: white; background: linear-gradient(90deg, #8B0000, #c53030); border-color: transparent; }
.btn-danger:hover { transform: translateY(-3px); box-shadow: 0 4px 20px rgba(255, 77, 77, 0.5); }
.btn-secondary { color: var(--text-primary); background: rgba(255, 255, 255, 0.05); border-color: var(--border-color); }
.btn-secondary:hover { background: rgba(255, 255, 255, 0.1); border-color: #8b5cf6; color: #8b5cf6; }
.btn[disabled] { background: #2a2a2a; color: #666; cursor: not-allowed; box-shadow: none; transform: none; }
.btn.btn-success[disabled] { background: rgba(34,197,94,0.15); color: #22c55e; border: 1px solid #22c55e; }
.btn.btn-expired[disabled] { background: rgba(239,68,68,0.15); color: #ef4444; border: 1px solid #ef4444; }

.carpool-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 2rem; }
.carpool-card { background: var(--card-bg); border: 1px solid var(--border-color); border-radius: 16px; padding: 1.5rem; backdrop-filter: blur(10px); display: flex; flex-direction: column; transition: all 0.4s ease; }
.carpool-card:hover { transform: translateY(-8px); box-shadow: 0 8px 25px rgba(0, 0, 0, 0.4), 0 0 20px rgba(158, 0, 255, 0.4); border-color: rgba(158, 0, 255, 0.5); }
.carpool-card h4 { font-size: 1.5rem; margin-bottom: 1rem; border-bottom: 1px solid var(--border-color); padding-bottom: 1rem; font-weight: 600; }
.carpool-card p { color: var(--text-secondary); margin-bottom: 0.75rem; }
.carpool-card p strong { color: var(--text-primary); font-weight: 500; }
.carpool-card .card-footer { margin-top: auto; padding-top: 1rem; display: flex; flex-wrap: wrap; gap: 0.5rem; }
.carpool-card .card-footer > * { flex-grow: 1; }

/* --- 4. PAGE-SPECIFIC STYLES --- */

/* Home Page / Hero */
.hero-container { display: grid; place-content: center; text-align: center; min-height: 80vh; position: relative; }
.hero-container h1 { font-size: 4rem; text-shadow: 0 6px 18px rgba(0, 0, 0, 0.35); margin-bottom: 1rem; line-height: 1.2; font-weight: 700; }
.hero-container p { font-size: 1.2rem; color: var(--text-secondary); max-width: 600px; margin: 1rem auto 2.5rem; }
.hero-container::after { content: ''; position: absolute; top: 50%; left: 50%; width: 620px; height: 620px; background: radial-gradient(circle, rgba(0, 162, 255, 0.08) 0%, rgba(0, 162, 255, 0) 52%); transform: translate(-50%, -50%); z-index: -1; pointer-events: none; filter: blur(12px); }
.hero-container .btn-primary { animation: pulse 2.5s infinite cubic-bezier(0.4, 0, 0.6, 1); padding: 16px 40px; font-size: 1.1rem; }

@keyframes pulse {
    0%, 100% { transform: scale(1); box-shadow: 0 4px 16px rgba(0, 0, 0, 0.35); }
    50% { transform: scale(1.02); box-shadow: 0 6px 22px rgba(0, 162, 255, 0.4); }
}

.benefits-section { padding: 4rem 0; }
.cta-section { text-align: center; background: var(--card-bg); border: 1px solid var(--border-color); border-radius: 16px; padding: 3rem; margin: 4rem auto; max-width: 800px; }
.cta-section h2 { font-size: 2.2rem; margin-bottom: 1rem; }
.cta-section p { color: var(--text-secondary); margin-bottom: 2rem; font-size: 1.1rem; }
        
/* Login & Register Page */
.auth-container { max-width: 900px; display: flex; background: var(--card-bg); border-radius: 20px; border: 1px solid var(--border-color); backdrop-filter: blur(15px); box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37); overflow: hidden; }
.auth-section { padding: 3rem; width: 50%; }
.auth-divider { display: flex; align-items: center; justify-content: center; position: relative; }
.auth-divider::before { content: ''; position: absolute; top: 20%; bottom: 20%; width: 1px; background: var(--border-color); }
        
/* Forms (Create Offer & Auth) */
.form-container { max-width: 700px; margin: 0 auto; background: var(--card-bg); border: 1px solid var(--border-color); border-radius: 16px; padding: 2.5rem; backdrop-filter: blur(10px); }
.form-group { margin-bottom: 1.5rem; }
.form-group label { display: block; margin-bottom: 0.5rem; color: var(--text-secondary); }
.form-group input, .form-group select { width: 100%; background: rgba(0, 0, 0, 0.2); border: 1px solid var(--border-color); border-radius: 8px; padding: 14px; font-size: 1rem; color: var(--text-primary); transition: all 0.3s ease; }
.form-group input:focus, .form-group select:focus { outline: none; border-color: var(--glow-blue); box-shadow: 0 0 15px rgba(0, 170, 255, 0.4); }
        
/* Chat Page */

.chat-container {
    max-width: 800px;
    height: 70vh;
    display: flex;
    flex-direction: column;
    background: var(--card-bg);
    border: 1px solid var(--border-color);
    border-radius: 16px;
    backdrop-filter: blur(10px);
    overflow: hidden;
}

#chat-box {
    flex-grow: 1;
    padding: 1.5rem;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
}

#chat-box p {
    background: linear-gradient(90deg, rgba(0, 170, 255, 0.18), rgba(158, 0, 255, 0.22));
    padding: 0.75rem 1.2rem;
    border-radius: 14px;
    color: var(--text-primary);
    max-width: 80%;
    width: fit-content;

    /* always left aligned */
    align-self: flex-start;

    /* soft glow */
    box-shadow: 0 0 6px rgba(0, 170, 255, 0.25);
}

#chat-box p strong {
    color: var(--glow-blue);
    font-weight: 600;
}

#chat-form {
    display: flex;
    padding: 1rem;
    border-top: 1px solid var(--border-color);
    gap: 1rem;
    background: rgba(10, 1, 34, 0.4);
    backdrop-filter: blur(10px);
}

#message-input {
    flex-grow: 1;
    padding: 12px 16px;
    font-size: 1rem;
    color: var(--text-primary);

    /* dark glassy input */
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(0, 170, 255, 0.3);
    border-radius: 10px;

    /* smooth animation */
    transition: border-color 0.25s ease, box-shadow 0.25s ease;
}

/* glow on focus */
#message-input:focus {
    outline: none;
    border-color: var(--glow-blue);
    box-shadow: 0 0 12px rgba(0, 170, 255, 0.4);
}

/* send button stays clean and modern */
#chat-form button {
    padding: 12px 24px;
    border-radius: 10px;
    font-weight: 600;
}


/* --- 5. RESPONSIVE ADJUSTMENTS --- */
@media (max-width: 768px) {
    .nav-container { flex-direction: column; gap: 1rem; }
    .hero-container h1 { font-size: 2.5rem; }
    .auth-container { flex-direction: column; max-width: 450px; }
    .auth-section { width: 100%; padding: 2rem; }
    .auth-divider::before { display: none; }
}
    </style>
</head>
<body>
    <%- include('../partials/header.ejs') %>
    <main>
        <%- body %>
    </main>
    <%- include('../partials/footer.ejs') %>
</body>
</html>